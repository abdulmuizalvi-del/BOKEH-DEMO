packages:
  "framer-motion": "For cinematic page transitions, smooth enter animations, and micro-interactions"
  "date-fns": "For formatting dates in mock data consistently"
  "clsx": "Utility for conditionally joining classNames"
  "tailwind-merge": "Utility for merging tailwind classes without style conflicts"

images:
  - path: "artifacts/bokeh/public/images/auth-bg.png"
    description: "Abstract cinematic dark background with subtle magenta and deep orange light leaks, smoky atmosphere, out of focus camera bokeh dots"
    aspect_ratio: "16:9"

notes:
  - No backend required for this MVP demo, all data is mocked.
  - Pure dark mode enforced in index.css.
